<p><strong>diff</strong> program output N = 1 with this <a href="iofile.php?test=<?=$SelectedTest;?>&amp;file=output">output file</a> to check your program is correct before contributing.
</p>

<p>Each program should print &quot;hello world&quot; and exit.</p>

<p>This benchmark measures startup costs. It is run differently than the others - each program is run N times in a loop by a shell script wrapper. We show the mean CPU time. Most <em>hello world</em> programs are too fast for <a
  href="faq.php#memory">the way we measure memory usage</a> so we only show CPU time.</p>
